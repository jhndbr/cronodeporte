# Database Configuration (Docker PostgreSQL)
DATABASE_URL="postgresql://cronodeporte:cronodeporte_secret@localhost:5432/cronodeporte_db?schema=public"
POSTGRES_USER="cronodeporte"
POSTGRES_PASSWORD="cronodeporte_secret"
POSTGRES_DB="cronodeporte_db"
POSTGRES_PORT=5432

# Authentication & Security
JWT_SECRET="cronodeporte_super_secret_jwt_key_development_32chars_min"
COOKIE_SECRET="crono_session_cookie_secret_salt"

# Optional AI API Key (falls back to deterministic analytical rules engine if absent)
GEMINI_API_KEY=""
